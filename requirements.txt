# =============================================================================
# Supply Chain Fraud Detection — Phase 1 (Local DirectRunner)
# =============================================================================

# Core stream-processing engine. Phase 1 is local-only so we skip the
# [gcp] extras (Pub/Sub, BigQuery IO) — add them back in Phase 2.
# GCP extras give us BigQuery + GCS + Dataflow runner support
apache-beam[gcp]==2.73.0

# Postgres driver (binary wheel — no libpq compile dance)
psycopg2-binary==2.9.10

# Mock data generation
Faker==30.3.0

# Timestamp parsing
python-dateutil==2.9.0

# Structured logging
structlog==24.4.0

# Test stack
pytest==8.3.3
pytest-mock==3.14.0
