# =============================================================================
# Supply Chain Fraud Detection — Phase 1 (Local DirectRunner)
# =============================================================================
# Pin only major/minor; allow patch upgrades so we still get CVE fixes.
# For prod Dataflow we'd freeze a lockfile with `uv pip compile`.
# -----------------------------------------------------------------------------

# Core stream-processing engine
apache-beam[gcp]==2.59.0          # GCP extras give us GCS/PubSub IO for free
                                  # in Phase 2 without re-installing.

# Postgres driver (binary wheel — no libpq compile dance locally)
psycopg2-binary==2.9.9

# Mock data generation (used by scripts/generate_mock_data.py only)
Faker==30.3.0

# Timestamp parsing helpers — Beam already pulls this, but we depend on it
# directly in transforms.py so we declare it explicitly (PEP 20: explicit > implicit).
python-dateutil==2.9.0

# Structured logging — nicer than the stdlib defaults for observability
structlog==24.4.0

# Test stack (only needed for `make test`)
pytest==8.3.3
pytest-mock==3.14.0
